const assert = require('assert')

describe('controllers.ReepayPayments.create-recurring-session', () => {

  it('should create a recurring Reepay Checkout session', async () => {
    // assert(false)
  })

})
