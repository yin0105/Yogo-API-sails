const assert = require('assert')

describe('controllers.ReepayPayments.create-order-and-charge-session', () => {

  it('should create an order, a charge session and return the charge session', async () => {
    // assert(false)
  })

})
