const assert = require('assert')

describe('controllers.ReepayPayments.attach-card-to-membership', () => {

  it('should accept and process webhook requests from Reepay', async () => {
    // assert(false)
  })

})
